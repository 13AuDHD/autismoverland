RewriteEngine On

# =========================================================
# COMMUNITY PROFILE ROUTES
# =========================================================

RewriteRule ^profile/([A-Za-z0-9_]{4,16})/?$ profile.php?username=$1 [L,QSA]


# =========================================================
# BADGE ROUTES
# =========================================================

RewriteRule ^badges/([a-z0-9-]+)/?$ badge.php?slug=$1 [L,QSA]


# =========================================================
# FORCE HTTPS
# =========================================================

RewriteCond %{HTTPS} !=on
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]


# =========================================================
# CUSTOM 404
# =========================================================

ErrorDocument 404 /404.html


# =========================================================
# CACHE CONTROL
# =========================================================

<IfModule mod_headers.c>

    <FilesMatch "\.(html?|php|json|js|css)$">
        Header always set Cache-Control "no-store, no-cache, must-revalidate, max-age=0"
        Header always set Pragma "no-cache"
        Header always set Expires "0"
    </FilesMatch>

    <FilesMatch "\.(png|jpg|jpeg|gif|webp|svg|ico|woff|woff2|ttf|otf)$">
        Header always set Cache-Control "public, max-age=86400"
    </FilesMatch>

    Header unset ETag

</IfModule>

FileETag None
