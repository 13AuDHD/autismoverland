/* =========================================================
   LLAMA SCOUT
   ACCESS-UI.JS

   Legacy compatibility file.

   Place access rendering is now handled directly by
   js/place.js using the accessLevel and locked-value
   structure returned by /api/places.php.

   This file intentionally does not override any Place
   renderer or formatting helpers.
   ========================================================= */


/*
 * Kept as an empty compatibility file so any older cached
 * page or template that still loads /js/access-ui.js does
 * not fail with a 404.
 *
 */
